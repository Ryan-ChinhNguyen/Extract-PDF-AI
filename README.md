# Extract-PDF-AI
This application provides an AI-powered PDF data extraction pipeline. Users can upload PDF documents, which are processed by an AI model to extract structured data. The system stores document metadata, processing status, and extraction results in a database.
